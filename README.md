# Algos

This repository contains short algorithm writeups and minimal, runnable
examples. Each algorithm lives in its own folder and is documented with a
single Markdown file plus optional small runners or tests.
